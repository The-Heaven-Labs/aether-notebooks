- [x] In the filesystem view, there should be some part dedicated to "recent"; *(batch 2)*
- [x] In the filesystem view, there should be some part that could search for notebooks via name or some part of the content; *(batch 2)*
- [x] In the filesystem view, there should be filters, like "things created by me" and similar *(batch 1)*
- [x] The Profile menu should appear when you click in the avatar/Name on the top right, not as a potential menu on the left sidebar; *(batch 1)*
- [x] The current user group appear as a label on the side of the Groups sidebar link. The list of groups someone belongs should appear in the profile instead; *(batch 1)*
- [x] In the chart config, there should be options to show labels, and to change colors of things *(batch 2)*
- [x] There is a menu for dashboards and connectors, but there is also a menu for notebooks that shows dashboards and connectors aswell. This is confusing. *(batch 2 — renamed nav to "Home")*
- [x] The new groups UI looks amazing, however, it is not the same visual style as other parts of the project. Create new components for the older parts of the project in this style within storybook so I can take a look and choose what I deem better; *(batch 3)*
- [x] The dark theme is just not implemented well. Do a complete quality implementation of a dark theme. Dont use colors that are too contrastant, e.g. #000000 or #FFFFFF, but some pastel easier on the eyes colors *(batch 2)*
- [x] When adding widgets to a dashboard, the components come in a "non-executed" state that has no data, evne markdown ones, asking to execute the notebook, but there is no option to execute anything; *(batch 2)*
- [x] In the filesystem views, the list should list not only the name, but who created and creation date. Optionally last update if you think it won't clutter things too much *(batch 3)*
- [x] There should be a default group "Everyone" or "All", for when someone wants to give permission to everyone regardless of what groups they have; *(batch 2)*
- [x] In the permissions menu there should be default configs for None, viewer, editor and admin which will tick different specific permissions, e.g. editor can view, use, edit, but not share and delete in the case of a connector; *(batch 1)*
- [x] I can't edit an already created connector via UI *(batch 1)*
- [x] On top of being able to run cells in a dashboard, there should be a way to create schedules so the dashboard contents will be updated from time to time. Add defaults e.g. 30s, 1m, 5m, 10m but also the option to use cron notation *(batch 2 — auto-refresh intervals implemented; cron schedules via SchedulesPanel from batch 1)*
- [x] In notebooks there should be an option to duplicate a cell *(batch 1)*
- [x] Changes on cell type are not saved in the history of a cell *(batch 2)*
- [x] There is no easy way to track cell updates from audit trail *(batch 2)*
- [x] When I click in one connector listed in the filesystem, it simply takes me to the /connectors pages since there is no edit for connectors *(batch 1)*
- [x] In the profile page, saving doesn't give user feedback that it worked or failed; *(batch 2)*
- [x] In the chart view, when switching between chart types, the selection is not persisted — when I close and re-open the same cell I lose my chart config; *(batch 3 — full config persisted to localStorage per cell)*
- [x] The chart config panel UX is bad. Redesign it to have a visual chart type selector with icons *(batch 2)*
- [x] In the presentation view the headings should not have those # anchor links shown, it breaks the UX *(batch 2)*
- [x] The "created by me" filter in Home only works for connectors, not for notebooks; *(batch 4 — verified working; was a past bug fixed in batch 2/3)*
- [x] There should be a "last updated" datetime shown in the filesystem view alongside creation date; *(batch 4)*
- [x] Dashboards are broken — widgets overlap, the grid is not configurable, and widgets are not draggable; *(batch 4 — smart placement for new widgets; view page now uses react-grid-layout)*
- [x] When the code cell has lots of results, it will be collapsed and a scroll bar added. This is the expected behavior. However, it would be good to be able to customize the size of the scrollable area by clicking and dragging. Also, the scroll do not follow the visual style of the rest of the application. *(batch 5 — drag resize handle + themed scrollbar)*
- [x] The series color for charts takes a very long time to apply, it should be instant or better, dynamic while you are looking for a nice color. Also, it should have a fully custom way to define colors, but having a few good defaults with nice looking colors matching the vibe of the app should be the default. *(batch 5 — instant local state + preset swatch row)*
- [x] Clicking Data-Labels button or any of the display settings  on chart config does nothing, or at least nothing instantly (maybe same problem with delay as above?) *(batch 5 — local state drives chart immediately)*
- [x] The blinking bar (that says where you are at) in the code cell with dark theme is black, thus I simply cant see it, has no contrast. *(batch 5 — theme.css explicit cursor color per mode)*
- [x] The default for presentation cell separation should be: cells are by default separate, the button make the one clicked be together with the previous one. Also, add a little more spacing from a markdown cell to a code cell in presentation view mode *(batch 5 — slide_break logic inverted; icon changed)*
- [x] I can add an markdown widget to a dashboard. That being the case, the content will not be displayed, it shows "No results yet, run the notebook first" *(batch 5 — markdown cells render source directly in both dashboard pages)*
- [x] Grid size in dashboard view should be configurable. When dragging/resizing a widget in the dashboard dark theme, the visual feedback for it should be other color than the current red *(batch 5 — cols picker 6/8/12/16/24; accent-colored drag placeholder)*
- [x] It should be possible to add cells directly from a notebook to a dashboard, instead of from dashboard only. Also, perhaps link a dashboard to a notebook to make this easier? *(batch 5 — "Add to dashboard" button on each cell opens searchable modal)*
- [x] The audit logs viewer in the web UI, for cell changes/creation, should show the parent name (notebook for cells) instead of only an UUID *(batch 5 — SQL join + ResourceCell component)*
- [x] I createed a clickhouse connection. When trying to run queries against it, I got: scan: clickhouse [ScanRow]: (event_time) converting DateTime to *interface {} is unsupported; scan: clickhouse [ScanRow]: (name) converting String to *interface {} is unsupported; (Add better tests and integrations with clickhouse, it will be our main destiny for database connections, thus, it should work flawlessly and be tested in every iteration of the development process) *(batch 5 — typed scan destinations per column type, Nullable and LowCardinality wrappers handled)*
- [x] When a cell returns an error in dark theme, the error modal is red and white (bad contrast), improve it for the dark theme. It works fine for light theme. *(batch 5 — error block uses CSS variables: --error-light, --error-border, --error-text)*
- [x] In light theme, the "Heaven's Notebooks" name on top left has wrong contrast in the light theme, not legible. Same for the org name on the top right. *(batch 5 — TopBar brand/org now use --nav-text which contrasts with the always-dark nav bar)*
- [ ] I may be mistaken, but while there is a button to login with SSO/OIDC, there is no way to configure it. Is that reading correct? *(confirmed: OIDC providers are startup-configured only via env/config; runtime UI configuration requires a new admin panel + hot-reload — pending)*
- [x] Changing the color now makes the application extremely slow and sometimes will "crash" with a white page *(batch 5 — color picker uses uncontrolled input + onBlur only; chart re-renders once when picker closes, not on every mouse move)*
- [x] Cursor for markdown cells seem to be light gray in dark theme, but for code it is still black *(batch 5 — same theme.css rule covers all .cm-editor instances)*
- [x] When clicking add to dashboard in a cell, there is no modal for search or similar. If there are lots of dashboards, this will be problematic soon *(batch 5 — dashboard picker modal)*
- [x] The audit login action should also log the IP where the user logged from. I guess this requires adding an "additional fields" or similar to record/show more information regarding specific actions. *(batch 5 — IP stored in metadata.ip, shown in audit row alongside user email)*
- [x] In the dark theme, the audit filters text have colors that do not contrast with the background. *(batch 5 — filterInput uses --bg-input and --text-primary)*
- [x] The filter by action text filter doesnt seem to work in the audit page *(batch 5 — changed from exact match to ILIKE %action% partial search in audit.go)*
- [x] The Groups page in the dark theme has no contrast for the cursor nor the text in the create new group input text *(batch 6 — input and renameInput now use --bg-input + --text-primary; border changed to --border)*
- [x] The "add member" dropdown uses the "system" dropdown. It should be a stylized one that matches the style of the app and not be system native, but rather web based completely *(batch 6 — replaced native <select> with custom MemberDropdown: searchable, keyboard-navigable, fully CSS-variable styled)*
- [x] Trying to run a query into a clickhouse query returns: scan: clickhouse [ScanRow]: (user_id) converting UInt32 to *uint64 is unsupported. try using *uint32; Query: SELECT * FROM test.events LIMIT 100; *(batch 6 — was already fixed in f09f841; test updated to use t.Skipf on connection failure)*
- [x] The cursor on the dark theme for code cells is still with wrong contrast. You should validate it directly before saying it is fixed. *(batch 6 — cursor color set inside EditorView.theme() using var(--text-primary), overrides CM's injected black)*
- [x] "SHOW TABLES" is not color highlighted in a code cell. *(batch 6 — switched default SQL dialect from StandardSQL to MySQL which includes SHOW/TABLES keywords)*
- [x] Color highlighting should be able to identify the type of connection inside the cell and use appropriate syntax understanding *(batch 6 — languageExtension() picks PostgreSQL/MySQL dialect by connector type; JS cells use lang-javascript)*
- [x] On the table results, it should be possible to click on the column names to order by it. It should respect the type of the column, if string alphabetically, if number smaller/greater. Order by should be NONE, ASC, DESC on these clicks *(batch 6 — sortable headers with ChevronUp/Down icons, numeric/string detection, nulls to bottom)*
- [x] Some column values are too big to directly show in the table view. When that's the case, there should be some default truncation, but the value should be clickable. When clicking, a new modal should appear on the right side of the cell or page showing the full value. This "navigation" should support moving from the keyboard, so pressing Down would select the next row and the focused value change with it *(batch 6 — 100-char truncation, right-side detail panel, keyboard Up/Down/Escape navigation)*
