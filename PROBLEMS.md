- [ ] Just hard refreshed a notebook page. I scroll down to the output of cell 3, select one cell. Instead of the data in output 3 being highlighted, the corresponding data in cell 1 is highlighted instead. The modal with the data detail opens correctly at cell 3. There is no way to select data from output of any cell, it always select from cell 1.
- [ ] The created MOTDs do not appear in the MOTD menu (but I can create and see them)
- [ ] MOTD do not appear in login page, even checking to do so in MOTD creation
- [ ] MOTDs have too muted colors, they should draw more attention. It is worse in light theme, but also bad in dark theme
- [ ] When creating a MOTD, I can set it to appear only in specific pages, but there is no way to configure which page
- [ ] When selecting the different tabs in the admin page, the tab selector feedback stay active even after changing tabs
- [ ] In theory agents are supported in any page, not only in notebooks page, but there is currently no way to trigger it. There should be a floating thing in the bottom right that when clicked, opens the modal (the modal should be collapsable AND closable)
- [ ] When just logging in, you land on `/`. If I refresh the page, I'll just then land in the user's home.
- [ ] Dashboard widget sizes are not persisted
- [ ] There are no indications wheter or not the state is saved when you make changes to a dashboard
- [ ] Currently it is not possible to create folders at root, since user will always be inside its own folder. It is not possible to move a folder to root either.
- [ ] The /admin menu has no way to go back to the previous/home page. It should show the menu and header just like all other pages.
- [ ] When you add permissions to a user/group, it is not possible to change permissions nor remove them. When you hover over, they all disappear, and when dont, clicking does nothing. (Correction: Click toggles the visibility of the permissions and remove button, but dont change them.)
- [ ] From user A, setting all permissions to user B to your (user A) folder doesnt show the folder to user B when showing the file tree
- [ ] Connector's permissions are not 100% working. While user B without permission will not be able to run queries, it will be able to navigate the whole schema via UI.
- [ ] Permissions are not correctly being applied to Agents/Models/MCPs/Skills. A newly invited to org user can edit all of them without explicit permissions. They should follow the ACL schema of other resources.
- [ ] Trying to open (directly via URL) a notebook from a user with no access shows not found, but should return a clear lack of permission error.
- [ ]
