- [x] Just hard refreshed a notebook page. I scroll down to the output of cell 3, select one cell. Instead of the data in output 3 being highlighted, the corresponding data in cell 1 is highlighted instead. The modal with the data detail opens correctly at cell 3. There is no way to select data from output of any cell, it always select from cell 1.
- [x] The created MOTDs do not appear in the MOTD menu (but I can create and see them)
- [x] MOTD do not appear in login page, even checking to do so in MOTD creation
- [x] MOTDs have too muted colors, they should draw more attention. It is worse in light theme, but also bad in dark theme
- [x] When creating a MOTD, I can set it to appear only in specific pages, but there is no way to configure which page
- [x] When selecting the different tabs in the admin page, the tab selector feedback stay active even after changing tabs
- [x] In theory agents are supported in any page, not only in notebooks page, but there is currently no way to trigger it. There should be a floating thing in the bottom right that when clicked, opens the modal (the modal should be collapsable AND closable)
- [x] When just logging in, you land on `/`. If I refresh the page, I'll just then land in the user's home.
- [x] Dashboard widget sizes are not persisted
- [x] There are no indications wheter or not the state is saved when you make changes to a dashboard
- [x] Currently it is not possible to create folders at root, since user will always be inside its own folder. It is not possible to move a folder to root either.
- [x] The /admin menu has no way to go back to the previous/home page. It should show the menu and header just like all other pages.
- [x] When you add permissions to a user/group, it is not possible to change permissions nor remove them. When you hover over, they all disappear, and when dont, clicking does nothing. (Correction: Click toggles the visibility of the permissions and remove button, but dont change them.)
- [x] From user A, setting all permissions to user B to your (user A) folder doesnt show the folder to user B when showing the file tree
- [x] Connector's permissions are not 100% working. While user B without permission will not be able to run queries, it will be able to navigate the whole schema via UI.
- [x] Permissions are not correctly being applied to Agents/Models/MCPs/Skills. A newly invited to org user can edit all of them without explicit permissions. They should follow the ACL schema of other resources.
- [x] Trying to open (directly via URL) a notebook from a user with no access shows not found, but should return a clear lack of permission error.
- [x] Changing a cell from cell to markdown makes it not selected/accessible to enter pressing enter, also pressing arrows doesnt navigate the cells, it "scrolls" instead, the default behavior. This seem to be a problem with the markdown cell specifically, not the change type behavior.
- [x] The icon for new code/text cell doesnt appear when hovering on top of the first cell.
- [x] When a dashboard is shared but the underlying notebooks are not, the user opens a dashboard which will have no access. There should be 2 sharing modes, one that shares it and the cell permissions are the ones from the person that opened it (received the share), and one where the user that receives the share can see/run the cells with the same permissions (for view/run) of the user that made the share/add permission.
- [x] The default AI panel inside notebooks is now getting half of the space. The horizontal size should be changeable, and the default should be closer to 1/3
- [x] Live data syncronization between different users in a notebook with changes seem to not be working, needing to refresh page to see changes from user 1, when logged as user 2.
- [x] Pressing A/B to create cells should focus these cells instead of keep on the previous selected one
- [x] Cant access the folder '/` anymore
- [x] When changing a cell from markdown to code, it doubles the content. Likely a sync issue.
- [x] When the notebook agent modal is open, the global AI toggle button is on top of it, making it hard to reach the cancel/send button
- [ ]
