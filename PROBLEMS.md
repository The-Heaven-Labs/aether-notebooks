- [x] There is now no way to collapse the filetree as there was in the past (at least I dont see it in the UI) - **FIXED & VALIDATED**: Corrected button positioning in TwoPanelLayout.tsx from `right: -14` to `left: collapsed ? -7 : leftWidth - 7`
- [x] The Edit button on the MCPs page has strange colors on the dark theme - **FIXED & VALIDATED**: Added missing `editBtn` style in MCPPage.tsx with proper accent color
- [x] Logged as Demon, when I go to the files page, I'm in the Angel's Home (which is the first that appears in the filetree) instead of the Demon's one. Also, I didnt gave permission to Demon on Angels home, why can I see notebooks there? - **FIXED & VALIDATED**: Updated HomePage.tsx to use `user.user_id` from useAuth hook to find the correct home folder by matching `owner_id`
- [x] The problem on dashboards where changing the column size on Edit mode resets to 12 columns in view mode still happens - **FIXED & VALIDATED**: Added missing `handleUpdateDashboard` handler in Go backend and PUT route for `/api/v1/dashboards/{id}` to save dashboard settings including `grid_cols`
- [x] Clicking the generated invite links with a new user still wont work, the token in the url will be lost after clicking to create the account, which shows the selection to create or join orgs - **FIXED & VALIDATED**: Updated LoginPage.tsx to check for pending join token in sessionStorage after login/registration and redirect to `/join` instead of `/`. Also updated JoinPage.tsx to use onboarding_token for API calls when user just registered. Also fixed duplicate home folder issue by adding check in `createHomeFolder` function.
- [x] Still can't see any docs/swagger in the UI - **FIXED & VALIDATED**: Added `/docs` and `/swagger.json` to Vite proxy configuration so Swagger UI is accessible through the frontend at `http://localhost:5173/docs`. Added API Documentation link to profile dropdown menu. Styled Swagger UI with clean light/dark themes using same CSS variables as main app (`--bg-primary`, `--text-primary`, `--accent`, etc.). Fixed all contrast issues using `!important` to override Swagger UI inline styles: method badges, paths, descriptions, info text, servers section, and buttons all have proper contrast. Added Toggle Theme button in top-right corner. Theme persists via localStorage.
- [x] Swagger/OpenAPI documentation not fully documented - **FIXED & VALIDATED**: Set up swag for auto-generation from code annotations. Added annotations to all handlers (auth, users, notebooks, cells, connectors, dashboards, members, folders, groups, permissions, tokens, schedules, templates, mcp-servers, model-configs). Documentation is now auto-generated and stays in sync with code. Run `swag init -g cmd/hnb-server/main.go -o internal/api/docs` to regenerate after changes.
- [x] Postgres and clickhouse support some type of comment/description for tables and columns, these should appear in the schema browser aswell - **FIXED & VALIDATED**: Added `Description` field to `ColumnInfo` and `TableInfo` structs. Updated Postgres `Schema()` to fetch table/column comments using `col_description()` and `obj_description()`. Updated ClickHouse `Schema()` to fetch column comments from `system.columns.comment` and table comments from `system.tables.comment`. Updated frontend `SchemaBrowser` and `TreeItem` components to display table descriptions as italic text under expanded tables, and column descriptions as small italic text under each column name. Also updated `explore_schema` agent tool to include descriptions in its output so agents can see table/column comments. Added comments to all PostgreSQL tables (39 tables) and ClickHouse tables (4 tables). Verified visually - both table and column descriptions show correctly in the schema browser UI.
- [x] The agent skills part seem to be providing more than the selected skill to the agent each time - **FIXED & VALIDATED**: Implemented progressive disclosure for skills following the Agent Skills specification. System prompt now only contains skill catalog in YAML format (name + description) instead of full skill content. Added `load_skill` tool for agents to load full skill instructions on-demand when a task matches a skill description. Kept `/skill:name` for explicit user invocation. Agent now auto-loads skills only when relevant, not on every turn.
- [ ] When the agent uses update_cell to change the cell source, it is not being reflected dinamically in the screen right away, I need to manually refresh the page for it to appear - **DESIGN READY**: Root cause identified: Yjs and database are dual sources of truth. See `docs/designs/yjs-source-of-truth.md` for the full design. **Solution**: Make Yjs the single source of truth using `ygo` library (Go Yjs implementation). Agent writes to Yjs first, then updates database cache. Auto-save suppressed after agent updates via `agent_updated_at` timestamp. Implementation pending.
- [x] Only folders user has access should appear in the home screen, currently I see all folders and can enter. If have no access to notebooks in it, see nothing there. More than this, I want that if I do not have access to the folder, it shouldnt appear at all in the home - **FIXED & VALIDATED**: Updated `handleListHomeFolders` and `handleListRootContents` in Go backend to filter folders based on whether user has access to at least one resource inside. Added database indexes for performance.
- [ ] There are still some confirmation dialogs that are using the operating system default instead of an app's stylized one. Place I found it: notebook's delete cell. Look for every place that might have this still and update it to use a custom app stylized modal instead.
- [ ] Notebook presentation mode support only cell output for code cells. It should support both source and output. To hide any of these, should use the hide source. There should also be a hide output/reset output for code cells individually aswell.
- [ ] The Last updated x time ago in the top right corner of notebooks doesnt seem to be updating correctly when changes happen to notebook cells
- [ ] Not every keyboard shortcut works in notebooks. Also, the behavior for navigation specifically between cells, how to enter/leave the source form them, change cell type etc is not working. I want this part to be the very same as it is in JupyterLab
- [ ] When using SHOW TABLES LIKE '%' in the opensearch connector, 10 columns appear, but the only ones that have data are TABLE_CAT (which always has docker-cluster in it) and TABLE_NAME (which actually has the table names). We need to improve this (either by removing these columns with no data, or adding more relevant data there if possible). Before doind any action here, I'd like an explanation as to why this is the current behavior.
- [ ] Brainstorming ideas: This notebook has a strong focus on security analysts. Would it be possible to have charts like timelines that show what happened over time? Or a chart that shows hierarchy of things, e.g. processes given by process IDs and their parents?
- [ ] 
