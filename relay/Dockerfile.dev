FROM node:22-alpine

# tsx provides fast TypeScript execution with --watch support
RUN npm install -g tsx

WORKDIR /app

COPY package*.json ./
RUN npm install && rm -rf /app/node_modules/.cache && chown -R node:node /app/node_modules

# Source is mounted at runtime
CMD ["tsx", "watch", "src/index.ts"]
